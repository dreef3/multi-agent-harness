{
  "name": "multi-agent-harness-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "bunx playwright test",
    "test:ui": "bunx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  }
}
